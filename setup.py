# drum-transcription-model/setup.py
from setuptools import setup, find_packages

setup(
    name="drum_model",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # e.g. "numpy>=1.24.0", "torch>=2.0.0", etc.
    ],
)
